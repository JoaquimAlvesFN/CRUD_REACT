import React from "react";
import Container from "../../components/Container/container.component";

export default function NoMatch() {
    return (
        <Container>
            <h1>No match, not found.</h1>
        </Container>
    );
}